language: python
sudo: false
python:
    - 2.7
    - 3.4
    - 3.5
    - 3.6
    - pypy-5.4
env:
    - ENVIRON=py
    - ENVIRON=isort,flake8,docs
matrix:
    exclude:
        - env: ENVIRON=isort,flake8,docs
    include:
        - python: "3.6"
          env: ENVIRON=isort,flake8,docs
install:
    - pip install tox coveralls coverage
script:
    - tox -e $ENVIRON
after_success:
    - coverage combine
    - coveralls
notifications:
    email: false
